- provide(:title, @school.name)

.row
  == render 'shared/sidebar'
  div.col.s8
    == render 'schools/profile'
    ul.school-chat
      - if @school_chats.any?
        == render @school_chats
      = paginate @school_chats